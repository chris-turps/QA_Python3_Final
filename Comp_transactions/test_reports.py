class Test_recordTrans:

    def test_foo(self):
        assert recordTrans(thistrans) == 2

    def test_fooFail(s):
        assert recordTrans(thistrans) == 2

# Bad example, since the script should ideally return a value to check against and recordTrans() does not return anytbing.
